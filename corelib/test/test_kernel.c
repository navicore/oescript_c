#include "config.h"
#undef NDEBUG
#include <assert.h>

#include "OEK.h"
//#include <oek_factory.h>

//todo: tests are defered to the protocol packages where enough junk exists to test everything.  someday
// this pkg needs mock impls
void test_subject_api(void) {

    //OEK kernel = oek_fnew();

    //oek_ffree(&kernel);
}

//suite
int main(void) {

    test_subject_api();

    return 0;
}

