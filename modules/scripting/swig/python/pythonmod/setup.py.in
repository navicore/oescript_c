from distutils.core import setup

setup(name = "oescript",
            version = "@VERSION@",
            py_modules = ['oescript', 'oescriptimpl'],
    )

#ejs todo: add a copy command that finds the *.so file and puts it in build/lib

